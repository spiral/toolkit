<div class="form-group col-${col|12} ${group-class}" attr:aggregate="prefix:field-">
    <button type="${type|submit}" class="btn btn-${class|primary}" attr:aggregate>
        ${label|Submit}
    </button>
</div>