import { resolveKeyPath } from './helpers/tools';
import type { ISFCore, ISFHelpers } from './types';
export declare const core: ISFCore;
export declare const helpers: ISFHelpers;
export declare const tools: {
    resolveKeyPath: typeof resolveKeyPath;
};
