import { bodyWrapper } from 'render/list/bodyWrapper';
import { cellRenderer } from 'render/list/cellRenderer';
import { footerWrapper } from 'render/list/footerWrapper';
import { headerCellRenderer } from 'render/list/headerCellRenderer';
import { headerWrapper } from 'render/list/headerWrapper';
import { rowWrapper } from 'render/list/rowWrapper';
import { tableWrapper } from 'render/list/tableWrapper';

export const listRenderers = {
  bodyWrapper,
  cellRenderer,
  footerWrapper,
  headerCellRenderer,
  headerWrapper,
  rowWrapper,
  tableWrapper,
};
