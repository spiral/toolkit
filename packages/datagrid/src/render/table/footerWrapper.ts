import { IBodyWrapperRenderer } from '../../types';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const footerWrapper: IBodyWrapperRenderer = (node, options, state) => undefined;
